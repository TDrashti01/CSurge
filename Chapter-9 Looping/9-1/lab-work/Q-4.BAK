#include <stdio.h>
#include <conio.h>

main()
{
	int n;

	clrscr();

	printf("\n\n\tEnter Any Number :");
	scanf("%d", &n);
	clrscr();

	if (n % 2 == 0)
		n--;

	while (n >= 1)
	{
		printf("%d.\t", n);
		n -= 2;
	}

	getch();
}
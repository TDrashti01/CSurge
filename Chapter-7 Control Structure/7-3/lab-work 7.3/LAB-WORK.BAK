#include <stdio.h>
#include <conio.h>

main ()
{
	int a, b, c, ans;

	clrscr();

	printf("\n\n\tEnter a value of the first number :");
	scanf("%d", &a);

	printf("\n\n\tEnter a value of the first number :");
	scanf("%d", &b);

	printf("\n\n\tEnter a value of the first number :");
	scanf("%d", &c);

       //	ans = (a < b) ? ((a < c) ? a : c) : ((b < c) ? b : c)
       //	printf("\n\tThe Minimum value is : %d", ans);

       ( a < b && a < c) ? printf("\n\tThe Minimum value is : %d", a)
	       : (b < c) ? printf("\n\tThe Minimum value is : %d", b)
			 : printf("\n\tThe Minimum value is : %d", c);

	getch();
}
#include <stdio.h>
#include <conio.h>

main ()
{
	int num;

	clrscr();

	printf("\n\n\tEnter a Number :");
	scanf("%d", &num);

	if (num == 0)
	{
		printf("\n\tZero");
	}
	else if (num > 0 && num %2 == 0)
	{
		printf("\n\tPositive Even Number");
	}
	else if (num > 0 && num % 2 != 0)
	{
		printf("\n\tPositive Odd Number");
	}
	else if (num < 0 && num %2 == 0)
	{
		printf("\n\tNegative Even Number");
	}
	else
	{
		printf("\n\tNegative Odd Number");
	}
	getch();
}
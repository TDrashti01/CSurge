#include <stdio.h>
#include <conio.h>

main ()
{
	int x, y, formula;

	clrscr();

	printf("Enter value od x\t: ");
	scanf("%d",&x);

	printf("Enter value of y\t: ");
	scanf("%d",&y);

	formula = (x * x * x) + (y * y * y) + 2 * x * y * (x + y);

	printf("Formula\t= %d", formula);

	getch();
}
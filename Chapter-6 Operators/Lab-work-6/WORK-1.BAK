#include <stdio.h>
#include <conio.h>

main()
{
	int x, y, formula;

	clrscr();

	printf("Enter value of x\t: ");
	scanf("%d",&x);

	printf("Enter value of y\t: ");
	scanf("%d",&y);

	formula = (x * x) + (2 * x * y) + (y * y);

	printf("Formula\t= %d", formula);

	getch();
}